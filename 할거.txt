
http://localhost:8080/swagger-ui/index.html#/


api 서버와 클라이언트 간 서비스 상호작용
query 데이터 조회 (읽기 전용)
mutaion 데이터 변경 요청(추가,수정,삭제)
hook 함수형 컴포넌트(api등)의 호출 결과를 상태로 관리.

setSearchParams url파라미터의 객체, set을 하면 url변경


@ModelAttribute
url파라미터를 자동으로 맵핑, get, post요청
@RequestBody
post, put 요청
@RequestParam
url파라미터나 폼데이터를 처리. get요청

style={{}}에서 중괄호{{를 두번 쓰는 이유는
자바스크립트 코드가 JSX 문법 내에서 실행될 때,
style={}내부의 {}를  React가 객체 형태로 CSS 스타일을 인식하기 위해서이다.




===할거===
Google Cloud Translation API로 번역

안녕 난 초보개발자야. 내가 보여주는 코드를 통해, 주석을 달며 한글로 자세히 설명해줘.

                <div>
                    <span>주문번호</span>
                    <span>주문명</span>
                    <span>총액</span>
                    <span>결제시간</span>
                    <span>결제상태</span>
                </div> 
해당 코드뿐만 아니라 다른 페이지에서도 번역이 가능한 함수를 만들려면 어떻게해야돼? 번역API를 사용해줘



data.items[0].products[0].id

menuData.menuPrice[0].size의 size도 onClick함수에 전달하고 싶어.
<button onClick={() => handleMenuInfoModalButtonOnClick(menuData.find(item => item.menuName === menu.name)?.menuId)}>상세<br></br>정보</button>




data.data[0].menuName



price를 확인해서 길이가 1보다 크면 스위치 보이기.
길이가 1보다 크면 스위치에 따라 M, L 선택.
스위치에 따라 M과 L 텍스트가 다르게 출력.